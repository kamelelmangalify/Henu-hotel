# Disable LiteSpeed Cache for Hotel Management Booking App
<IfModule LiteSpeed>
  CacheLookup off
</IfModule>

# Disable mod_expires inheritance from parent WordPress
<IfModule mod_expires.c>
  ExpiresActive Off
</IfModule>

# Set strict no-cache headers for all files
<IfModule mod_headers.c>
  Header always set Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0"
  Header always set Pragma "no-cache"
  Header always set Expires "0"
</IfModule>
